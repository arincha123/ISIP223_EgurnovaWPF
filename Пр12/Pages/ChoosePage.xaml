<Page x:Class="Пр12.Pages.ChoosePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Пр12.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ChoosePage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="{Binding Name}" FontSize="30" FontWeight="Bold"/>
        </StackPanel>

        <ListBox x:Name="ListBoxMasters" Grid.Row="1" d:ItemsSource="{d:SampleData ItemCount=5}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="4" Margin="5" Padding="10">
                        <StackPanel>
                            <TextBlock FontSize="26" Width="auto">
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}{0} {1}">
                                        <Binding Path="FirstName" />
                                        <Binding Path="LastName" />
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                            <Button x:Name="BtnChoiceMaster" Width="150"  Content="Выбрать" FontSize="20" Click="BtnChoiceMaster_Click"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>

            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
        
        <ListBox x:Name="ListBoxServices" Grid.Row="2" d:ItemsSource="{d:SampleData ItemCount=5}" >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="LightGray" CornerRadius="5" BorderThickness="1" Margin="5" Padding="10">
                        <StackPanel>
                            <RadioButton x:Name="RadioService" Content="{Binding Name}" GroupName="GroupServices" FontSize="20" Margin="10 0 0 0" Checked="RadioService_Checked" />
                            <TextBlock Text="{Binding Description}" FontSize="20" Margin="30 0 0 0"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Focusable" Value="False" />
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

    </Grid>

    <!--<Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="10">
            <TextBlock Text="{Binding Name}" FontSize="30" FontWeight="Bold"/>
            <TextBlock Text="{Binding Description}" FontSize="22"/>
        </StackPanel>

        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="0,1,0,0" Margin="0,10,0,0">
            <StackPanel>
                <TextBlock Text="Выберите услугу:" FontSize="24" FontWeight="Bold" Margin="10,10,0,5"/>
                <ListBox x:Name="ListBoxServices" d:ItemsSource="{d:SampleData ItemCount=5}" 
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="LightGray" CornerRadius="5" BorderThickness="1" Margin="5" Padding="10">
                                <StackPanel>
                                    <RadioButton x:Name="RadioService" Content="{Binding Name}" 
                                             GroupName="GroupServices" FontSize="20" Margin="10 0 0 0" 
                                             Checked="RadioService_Checked" />
                                    <TextBlock Text="{Binding Description}" FontSize="18" Margin="30 5 0 10"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Focusable" Value="False" />
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="0,1,0,0" Margin="0,10,0,0">
            <StackPanel>
                <TextBlock Text="Выберите мастера:" FontSize="24" FontWeight="Bold" Margin="10,10,0,5"/>
                <ListBox x:Name="ListBoxMasters" d:ItemsSource="{d:SampleData ItemCount=5}"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="4" Margin="5" Padding="10">
                                <StackPanel>
                                    <TextBlock FontSize="22" Margin="10,0,0,10">
                                        <TextBlock.Text>
                                            <MultiBinding StringFormat="{}{0} {1}">
                                                <Binding Path="FirstName" />
                                                <Binding Path="LastName" />
                                            </MultiBinding>
                                        </TextBlock.Text>
                                    </TextBlock>
                                    <Button x:Name="BtnChoiceMaster" Width="150" Height="35" 
                                        Content="Выбрать" FontSize="18" Click="BtnChoiceMaster_Click"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                </ListBox>
            </StackPanel>
        </Border>
    </Grid>-->

</Page>